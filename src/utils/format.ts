import type { CurrencyCode } from "@/constants/currencies";

export function formatMoney(amountMinor: string, currency: CurrencyCode): string {
  throw new Error(`Not implemented: formatMoney(${amountMinor}, ${currency})`);
}
