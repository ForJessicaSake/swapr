export function calculateFee(sellAmount: string): string {
  throw new Error(`Not implemented: calculateFee(${sellAmount})`);
}

export function applySpread(midMarketRate: string): string {
  throw new Error(`Not implemented: applySpread(${midMarketRate})`);
}
